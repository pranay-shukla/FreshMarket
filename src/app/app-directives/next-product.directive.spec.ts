import { NextProductDirective } from './next-product.directive';

describe('NextProductDirective', () => {
  it('should create an instance', () => {
    const directive = new NextProductDirective();
    expect(directive).toBeTruthy();
  });
});
