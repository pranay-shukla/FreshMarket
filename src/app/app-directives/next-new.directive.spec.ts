import { NextNewDirective } from './next-new.directive';

describe('NextNewDirective', () => {
  it('should create an instance', () => {
    const directive = new NextNewDirective();
    expect(directive).toBeTruthy();
  });
});
