import { PrevProductDirective } from './prev-product.directive';

describe('PrevProductDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevProductDirective();
    expect(directive).toBeTruthy();
  });
});
