import { PrevFeaturedDirective } from './prev-featured.directive';

describe('PrevFeaturedDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevFeaturedDirective();
    expect(directive).toBeTruthy();
  });
});
