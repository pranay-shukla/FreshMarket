import { PrevNewDirective } from './prev-new.directive';

describe('PrevNewDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevNewDirective();
    expect(directive).toBeTruthy();
  });
});
