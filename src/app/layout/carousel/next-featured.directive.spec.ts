import { NextFeaturedDirective } from './next-featured.directive';

describe('NextFeaturedDirective', () => {
  it('should create an instance', () => {
    const directive = new NextFeaturedDirective();
    expect(directive).toBeTruthy();
  });
});
